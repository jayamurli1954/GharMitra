[phases.setup]
nixPkgs = ["python312"]

[phases.install]
# Force rebuild - ensure asyncpg is installed (no-cache)
cmds = ["pip install --upgrade pip setuptools wheel", "pip install --no-cache-dir --prefer-binary -r requirements.txt"]
